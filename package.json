{
  "name": "luggage",
  "version": "0.0.1",
  "description": "Dropbox file API wrapper for storing json data.",
  "main": "dist/Luggage.js",
  "scripts": {
    "test": "mocha",
    "test-watch": "mocha --watch",
    "build": "babel --source-maps inline --optional runtime -d dist/ src/",
    "watch": "babel --watch --source-maps inline --optional runtime -d dist/ src/",
    "prepublish": "npm run build",
    "lint": "eslint src"
  },
  "directories": {
    "lib" : "dist",
    "test" : "test"
  },
  "author": "dra1n",
  "license": "ISC",
  "devDependencies": {
    "babel": "^5.6.23",
    "babel-eslint": "^3.1.30",
    "chai": "^3.2.0",
    "eslint": "^0.24.1",
    "eslint-config-future": "^2.1.1",
    "eslint-plugin-react": "^2.7.1",
    "mocha": "^2.2.5"
  },
  "dependencies": {
    "deep-equal": "^1.0.0"
  }
}
